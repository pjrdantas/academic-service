spring:
  application:
    name: academic-service

  datasource:
    url: jdbc:postgresql://localhost:5432/db_gestao_escolar
    username: postgres
    password: root123

    jpa:
      hibernate:
        ddl-auto: update
    show-sql: true